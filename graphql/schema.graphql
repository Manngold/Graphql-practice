type Movies {
    id: Int!
    title: String!
    star: Int!
    actor: String!
}

type Query {
    movies: [Movies]!
    movie(id: Int!): Movies
}

type Mutation {
    addMovie(title: String!, star: Int!, actor: String!): Movies!
    deleteMovie(id: Int!): Boolean!
}